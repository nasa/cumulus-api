machine:
  services:
    - docker

dependencies:
  pre:
    - pip install --upgrade pip
    - pip install docker-compose
  override:
    - npm install
    - docker-compose up local

test:
  override:
  - npm run test
