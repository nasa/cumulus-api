machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker run -d -p 8000:8000 --name dynamodb peopleperhour/dynamodb
    - npm install -g serverless@beta
    - npm install

test:
  override:
    - npm run test
