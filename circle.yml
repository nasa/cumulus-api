machine:
  services:
    - docker

dependencies:
  override:
    - docker run -d -p "8000:8000" peopleperhour/dynamodb
    - docker run -d -p "9324:9324" vsouza/sqs-local
    - npm install

test:
  override:
  - npm run test
